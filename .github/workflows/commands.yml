on:
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest 
    steps:
    - name: set API Key
      run: echo "API_KEY=1234" >> $GITHUB_ENV
    - name: use api key
      run: echo "using api key $GITHUB_ENV"

