on:
  push:
    branches:
      - main
      - master
      - "release/**"
    paths:
      - "src/**"
  pull_request:
    branches:
      - main
      - master
      - "release/**"
jobs:
  runs-on: ubuntu-latest
  steps:
  - name: Checkout code
    uses: actions/checkout@v7.0.0
  - name: run testche
    run: npm test

      
